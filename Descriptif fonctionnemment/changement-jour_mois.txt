nouveau jour :
	Vérifier dans toutes les catégories si il y a une nouvelle entrée automatique
	dépense / income : 
	if(is_stable && day_nb == today)
		new expense from last of the categorie
	idem dette (implied is_stable)	
	si nouveau solde < 0, message d'alerte
	rendre non modifiable les dépenses de la veille	

Nouveau mois :
si solde > 0 :
	répartir dans les savings 
	if (percentage)
		répartition du solde selon le pourcentage de chaque catégorie
		si une catégorie > max_amount
			somme (pourcentages) < 100 
			--> popup + redirection vers écran savings pour modifier pourcentages
	else
		répartition selon ordre de priorité 
	pop up récapitulative
sinon :
	popup avertissement

	
